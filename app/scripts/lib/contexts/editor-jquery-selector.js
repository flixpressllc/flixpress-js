// to depend on a bower installed component:
// define(['components/componentName/file'])

define([

], function() {

  return function (selector) {return $('#cboxWrapper iframe').find(selector);};

});
